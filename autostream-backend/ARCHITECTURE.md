# AutoStream Backend - Architecture Overview

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        FastAPI Server                            │
│                      (app/main.py)                              │
└────────────┬────────────────────────────────────────────────────┘
             │
             │ /api/chat
             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      API Endpoint                                │
│                      (app/api.py)                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  1. Get/Create Session (session_id)                       │  │
│  │  2. Add user message to state                            │  │
│  │  3. Run LangGraph workflow                               │  │
│  │  4. Return AI response + state                           │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────┬────────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   LangGraph Workflow                             │
│                   (app/agent/graph.py)                          │
│                                                                  │
│   ┌──────────┐      ┌──────────┐      ┌──────────────────┐    │
│   │  Intent  │─────▶│   RAG    │─────▶│ Plan Selection   │    │
│   │  Node    │      │  Node    │      │     Node         │    │
│   └──────────┘      └──────────┘      └──────────────────┘    │
│        │                  │                      │              │
│        ▼                  ▼                      ▼              │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │              Response Generation Node                    │  │
│   │  • Chooses appropriate prompt template                  │  │
│   │  • Generates reply using Gemini                         │  │
│   │  • Extracts info (name, email, platform)               │  │
│   └─────────────────────────────────────────────────────────┘  │
│                           │                                     │
│                           ▼                                     │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │           Lead Capture Node (Conditional)                │  │
│   │  • Executes ONLY if all fields present                  │  │
│   │  • Intent = high_intent                                  │  │
│   │  • Not already captured                                  │  │
│   └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Supporting Components                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Intent Classifier (app/agent/intent.py)               │    │
│  │  • Uses Gemini with low temperature                    │    │
│  │  • Returns: greeting | product_pricing | high_intent   │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  RAG Pipeline (app/agent/rag.py)                       │    │
│  │  • FAISS vector store                                   │    │
│  │  • Gemini embeddings                                    │    │
│  │  • Retrieves top-K contexts from knowledge.md          │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Lead Capture Tool (app/agent/tools.py)                │    │
│  │  • Validates all required fields present               │    │
│  │  • Logs lead to console (production: CRM)              │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Session Store (app/memory/session_store.py)           │    │
│  │  • In-memory LRU cache                                  │    │
│  │  • Per-session state isolation                          │    │
│  │  • 1-hour timeout, max 6 turns                         │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Prompts (app/agent/prompts.py)                        │    │
│  │  • All LLM prompt templates                             │    │
│  │  • Separated from business logic                        │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Conversation Flow

```
User: "Hi there!"
    │
    ├─▶ Intent: greeting
    ├─▶ RAG: Retrieve welcome message context
    └─▶ Response: "Welcome to AutoStream! ..."

User: "What are your pricing plans?"
    │
    ├─▶ Intent: product_pricing
    ├─▶ RAG: Retrieve pricing information from knowledge.md
    └─▶ Response: "We offer Basic ($29) and Pro ($79)..."

User: "I want the Pro plan. I'm Sarah."
    │
    ├─▶ Intent: high_intent
    ├─▶ Extract: name="Sarah"
    ├─▶ Update state: selected_plan="pro", name="Sarah"
    └─▶ Response: "Great choice, Sarah! Pro gives you unlimited exports..."

User: "My email is sarah@example.com, I create on YouTube"
    │
    ├─▶ Extract: email="sarah@example.com", platform="YouTube"
    ├─▶ Check: All fields present? YES
    ├─▶ Execute: Lead Capture Tool ✅
    │       ┌──────────────────────────────────────┐
    │       │ 🎯 LEAD CAPTURED SUCCESSFULLY        │
    │       │ Name: Sarah                          │
    │       │ Email: sarah@example.com             │
    │       │ Platform: YouTube                    │
    │       │ Selected Plan: PRO                   │
    │       └──────────────────────────────────────┘
    └─▶ Response: "Thanks Sarah! You'll receive an email at sarah@example.com..."
```

## 🎯 State Transitions

```
AgentState {
  messages: []                    →  [HumanMessage, AIMessage, ...]
  intent: "greeting"              →  "product_pricing" → "high_intent"
  selected_plan: null             →  "pro"
  name: null                      →  "Sarah"
  email: null                     →  "sarah@example.com"
  platform: null                  →  "YouTube"
  yt_channel: null                →  "youtube.com/@sarahtech"
  lead_captured: false            →  true ✅
  turn_count: 0                   →  4
}
```

## 🧩 Module Dependencies

```
main.py
  ├── api.py
  │     ├── graph.py
  │     │     ├── intent.py (Gemini)
  │     │     ├── rag.py (FAISS + Gemini Embeddings)
  │     │     ├── tools.py (Lead Capture)
  │     │     ├── prompts.py (Templates)
  │     │     └── state.py (TypedDict)
  │     └── session_store.py (Memory)
  └── config.py (Environment)
```

## 🚀 Deployment Architecture (Future)

```
┌─────────────────────────────────────────────────────────────────┐
│                         Load Balancer                            │
└────────┬───────────────────────────────────────┬─────────────────┘
         │                                       │
    ┌────▼─────┐                          ┌─────▼────┐
    │ FastAPI  │                          │ FastAPI  │
    │ Instance │                          │ Instance │
    │    1     │                          │    2     │
    └────┬─────┘                          └─────┬────┘
         │                                       │
         └───────────────┬───────────────────────┘
                         │
                    ┌────▼─────┐
                    │  Redis   │  ← Session Store
                    │  Cluster │
                    └────┬─────┘
                         │
         ┌───────────────┼───────────────────────┐
         │               │                       │
    ┌────▼─────┐   ┌────▼──────┐          ┌─────▼────┐
    │PostgreSQL│   │   CRM     │          │  Email   │
    │  (Leads) │   │(HubSpot/  │          │ Service  │
    │          │   │Salesforce)│          │(SendGrid)│
    └──────────┘   └───────────┘          └──────────┘
```

## 💪 Why This Architecture?

### ✅ Modular
- Each component has single responsibility
- Easy to test individual nodes
- Can swap Gemini for GPT-4 without rewriting logic

### ✅ Stateful
- LangGraph manages conversation state automatically
- Session store enables multi-user support
- Memory retention for context-aware responses

### ✅ Scalable
- Stateless API (state in Redis for production)
- Horizontal scaling with load balancer
- Async-ready (FastAPI + async functions)

### ✅ Maintainable
- Prompts separated from code
- Clear separation of concerns
- Type-safe with Pydantic/TypedDict

### ✅ Production-Ready
- Error handling at every layer
- Validation (Pydantic schemas)
- Monitoring hooks (easily add logging/metrics)
- Security (API keys in environment, no hardcoded secrets)

---

**This architecture powers a conversational AI that feels human while systematically qualifying and capturing leads.** 🎯
